<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.timetable.risfan_shekh.timetable">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".clas" />
        <activity android:name=".assignment" />
        <activity android:name=".exam" />
        <activity android:name=".teacher" />
        <activity android:name=".class_add" />
        <activity android:name=".assignment_add" />
        <activity android:name=".exam_add" />
        <activity android:name=".teacher_add" />
        <activity android:name=".about_us"></activity>
    </application>

</manifest>